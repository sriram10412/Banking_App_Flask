version: 0.2
env:
  variables:
    TF_VERSION: "1.7.5"
    AWS_DEFAULT_REGION: "ap-southeast-1"
    AWS_ACCOUNT_ID: "603196661038"
phases:
  install:
    runtime-versions:
      python: 3.12
    commands:
      - wget -q https://releases.hashicorp.com/terraform/${TF_VERSION}/terraform_${TF_VERSION}_linux_amd64.zip
      - unzip -q terraform_${TF_VERSION}_linux_amd64.zip
      - mv terraform /usr/local/bin/
      - pip install -q -r requirements.txt
      - yum install -y jq
  pre_build:
    commands:
      - aws ecr get-login-password --region ${AWS_DEFAULT_REGION} | docker login --username AWS --password-stdin ${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_DEFAULT_REGION}.amazonaws.com
      - IMAGE_TAG=${CODEBUILD_RESOLVED_SOURCE_VERSION}
      - IMAGE_URI=${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_DEFAULT_REGION}.amazonaws.com/prod-banking-app:${IMAGE_TAG}
  build:
    commands:
      - terraform init -backend-config="region=${AWS_DEFAULT_REGION}" -upgrade
      - terraform validate
      - terraform plan -var-file="environments/prod/terraform.tfvars" -var="app_image_tag=${IMAGE_TAG}" -out=tfplan
      - terraform apply -auto-approve tfplan
      - docker build -t ${IMAGE_URI} -t ${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_DEFAULT_REGION}.amazonaws.com/prod-banking-app:latest .
      - docker push ${IMAGE_URI}
      - docker push ${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_DEFAULT_REGION}.amazonaws.com/prod-banking-app:latest
  post_build:
    commands:
      - aws ecs describe-task-definition --task-definition prod-banking-app --region ${AWS_DEFAULT_REGION} > task-def.json
      - cat task-def.json | jq '.taskDefinition | del(.taskDefinitionArn, .revision, .status, .requiresAttributes, .placementConstraints, .compatibilities, .registeredAt, .registeredBy)' > new-task-def.json
      - NEW_TASK_ARN=$(aws ecs register-task-definition --region ${AWS_DEFAULT_REGION} --cli-input-json file://new-task-def.json --query 'taskDefinition.taskDefinitionArn' --output text)
      - aws ecs update-service --cluster prod-banking-cluster --service prod-banking-service --task-definition ${NEW_TASK_ARN} --force-new-deployment --region ${AWS_DEFAULT_REGION}
      - aws ecs wait services-stable --cluster prod-banking-cluster --services prod-banking-service --region ${AWS_DEFAULT_REGION}
artifacts:
  files:
    - tfplan
